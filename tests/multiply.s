
    # ---------------------------------------------------------
    # Test 1: 10 * 20
    # ---------------------------------------------------------
    li x1, 10
    li x2, 20

    mul     x3, x1, x2      # 200
    mulh    x4, x1, x2      # 0
    mulhu   x5, x1, x2      # 0
    mulhsu  x6, x1, x2      # 0

    # ---------------------------------------------------------
    # Test 2: -1 * 2
    # ---------------------------------------------------------
    li x1, -1
    li x2, 2

    mul     x7,  x1, x2     # 0xFFFFFFFE
    mulh    x8,  x1, x2     # 0xFFFFFFFF
    mulhu   x9,  x1, x2     # 0x00000001
    mulhsu  x10, x1, x2     # 0xFFFFFFFF

    # ---------------------------------------------------------
    # Test 3: 0x80000000 * 2
    # ---------------------------------------------------------
    li x1, 0x80000000
    li x2, 2

    mul     x11, x1, x2
    mulh    x12, x1, x2
    mulhu   x13, x1, x2
    mulhsu  x14, x1, x2

    # ---------------------------------------------------------
    # Test 4: 0xFFFFFFFF * 0xFFFFFFFF
    # ---------------------------------------------------------
    li x1, -1
    li x2, -1

    mul     x15, x1, x2
    mulh    x16, x1, x2
    mulhu   x17, x1, x2
    mulhsu  x18, x1, x2

done:
    j done